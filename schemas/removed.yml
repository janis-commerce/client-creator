paths:
  /listener/id/client/removed:
    post:
      x-janis-namespace: id-client
      x-janis-method: removed-listener
      tags:
        - Client
      summary: Client removed event listener
      security:
        - ApiKey: []
          ApiSecret: []
      description: Client removed event listener
      operationId: clientRemovedListener
      requestBody:
        required: true
        content:
          application/json:
            schema:
              description: A JANIS Event object
              type: object
              properties:
                service:
                  $ref: "#/components/schemas/listenerEvent/properties/service"
                entity:
                  $ref: "#/components/schemas/listenerEvent/properties/entity"
                event:
                  $ref: "#/components/schemas/listenerEvent/properties/event"
                client:
                  $ref: "#/components/schemas/listenerEvent/properties/client"
              required:
                - service
                - entity
                - event
                - client
      responses:
        "200":
          $ref: "#/components/responses/GenericSuccess"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
